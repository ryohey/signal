{
  "name": "pipedream-2.0",
  "version": "0.0.1",
  "description": "PipeDream 2.0 - A simple and enjoyable music creation tool with AI assistance.",
  "scripts": {
    "start": "turbo run dev --parallel",
    "build": "npm run build:app",
    "build:app": "turbo build && cpy \"app/dist/**/*\" dist --parents",
    "serve": "npx http-server dist",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "turbo format",
    "firebase": "npm --prefix functions run build && firebase emulators:start",
    "firebase:deploy": "firebase deploy",
    "dev:electron": "concurrently \"npm run dev -w app\" \"npm run dev --prefix electron\"",
    "build:electron": "npm run build:electron -w app && rimraf electron/dist_renderer && cpy \"app/dist/**/*\" electron/dist_renderer --parents",
    "package:electron:darwin": "npm run build:electron && npm run package:darwin --prefix electron",
    "package:electron": "npm run build:electron && npm run package --prefix electron",
    "make:electron": "npm run build:electron && npm run make:mas --prefix electron",
    "make:darwin": "npm run build:electron && npm run make:darwin --prefix electron",
    "make:win": "npm run build:electron && npm run make:win --prefix electron"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ryohey/signal.git"
  },
  "author": "ryohey",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ryohey/signal/issues"
  },
  "homepage": "https://github.com/ryohey/signal/",
  "private": true,
  "devDependencies": {
    "cpy-cli": "^4.2.0",
    "prettier": "^3.5.3",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.2"
  },
  "workspaces": [
    "app",
    "packages/*"
  ],
  "packageManager": "npm@11.3.0",
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "firebase": "12.6.0",
    "optics-ts": "^2.4.1",
    "vite": "7.2.7"
  }
}